# 修复身份证照片缩略图显示问题 - 实施计划

## [x] 任务 1: 修复前端认证材料缩略图显示

**优先级**: 高

**描述**:
1. 修改 AuditList.tsx 中认证材料列的渲染逻辑
2. 使用原生 `<img>` 标签替代 Ant Design 的 `<Image>` 组件显示缩略图
3. 使用 `Image.PreviewGroup` 包裹以保留点击预览大图功能
4. 调整缩略图尺寸和样式，确保SVG图片正确显示

**实现要点**:
- 列表页缩略图：使用原生 `<img>` 标签，设置固定尺寸 52x36
- 展开详情区：MaterialCard组件同样使用原生 `<img>` 标签
- 保持 `Image.PreviewGroup` 包裹以支持点击预览
- 添加图片加载失败的fallback处理

**验收标准**: AC-1, AC-2, AC-3

**测试要求**:
- `programmatic` TR-1.1: 检查列表页认证材料列是否使用原生img标签
- `programmatic` TR-1.2: 检查点击缩略图是否能正常打开大图预览
- `human-judgement` TR-1.3: 在浏览器中验证所有认证材料缩略图正常显示

**注意**: 保持代码风格与现有代码一致


## [x] 任务 2: 验证修复效果

**优先级**: 高

**描述**:
1. 刷新浏览器页面
2. 检查认证材料列的缩略图显示
3. 点击缩略图验证大图预览功能

**验收标准**: AC-1, AC-2, AC-3

**测试要求**:
- `programmatic` TR-2.1: 页面能正常加载无错误
- `human-judgement` TR-2.2: 身份证正面缩略图正常显示
- `human-judgement` TR-2.3: 身份证反面缩略图正常显示
- `human-judgement` TR-2.4: 手持身份证缩略图正常显示
- `human-judgement` TR-2.5: 点击缩略图能打开大图预览
