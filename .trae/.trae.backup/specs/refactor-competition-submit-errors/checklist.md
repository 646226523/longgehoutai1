# 验证清单

## 提交校验逻辑（CompetitionForm.tsx）
- [x] `collectIssues()` 覆盖：赛事名称 / 起点 / 终点 / 赛线合法性（起终点相同、超中国范围、空距异常）/ 手动空距（启用且非正数）
- [x] 每条错误信息采用「字段名 + 原因 + 修正指引」格式，后台工作人员可直接定位出错字段
- [x] `handleSubmit` 在存在问题时终止提交，不再发起后端请求
- [x] 错误弹窗同时触发 AntD 表单内联校验，对应字段下方标红
- [x] 右侧 `routeWarning` 实时 Alert 提示保留

## 错误弹窗 UI
- [x] 弹窗标题明确（如"提交失败，请完善以下信息"）
- [x] 弹窗正文以列表逐条展示全部问题（非仅单条）
- [x] 弹窗可正常关闭，不影响再次编辑后重新提交

## 提交成功路径
- [x] 所有校验通过时无错误弹窗，正常提交并提示成功

## 质量与构建
- [x] 前端 `npx tsc --noEmit` 零错误
- [x] 前端 `npm run build` 成功
- [x] 浏览器验证各错误场景弹窗内容清晰、字段定位准确（未填名称 / 缺起点 / 缺终点 / 起终点相同 / 超范围 / 手动空距无效 / 全部正确）
