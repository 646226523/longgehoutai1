# UICraft Design Context — 赛鸽基因溯源平台

## Design Context

### Users
- **平台管理员**: 负责全平台的公棚管理、赛事审核、基因档案维护、内容发布
- **公棚运营人员**: 管理单一公棚的鸽子、赛事、拍卖、检测
- **赛事调度员**: 专注赛事核验、实时飞行数据监控、归巢结果处理

使用场景：日常后台运营（浅色模式）+ 大屏指挥调度（暗色模式）。核心目标是让管理员"一屏看全局，一眼识重点，三分钟内完成决策和操作"。

### Brand Personality
- **专业**: 数据准确、操作可靠、权限严密
- **技术感**: 科技驱动，基因溯源、实时追踪、AI辅助
- **简洁高效**: 3分钟内完成核心决策操作，不啰嗦
- **指挥舱/战场感**: 大屏中控像作战指挥中心，关键指标一眼可见
- **优雅品质**: 高端鸽友俱乐部/拍卖场景需要品质感

### Aesthetic Direction
- **主题**: 浅色为主（日常后台）+ 暗色大屏（中控数据中台）。普通管理页面使用 Ant Design 浅色主题；中控大屏使用独立暗色科技主题。
- **配色**:
  - **主色调**: 青蓝色 `#00d4ff`（accentCyan）+ 金色 `#ffcc00`（accentGold）
  - **暗色背景**: `#0a1128`（bgPrimary）/ `#1a2332`（bgCard）/ `#2a3a5a`（border）
  - **暗色文字**: `#ffffff`（textPrimary）/ `#8ba3c7`（textSecondary）
  - **状态色**: 飞行中 `#52c41a`（绿）/ 归巢中 `#1890ff`（蓝）/ 已归巢 `#8c8c8c`（灰）/ 异常 `#ff4d4f`（红）
  - **浅色主色**: Ant Design `#1677ff`（用于非大屏页面）
- **字体**: PingFang SC, 微软雅黑, 系统字体栈。数据展示使用等宽字体。
- **视觉参考**: 数据可视化大屏风格（DataV、阿里云大屏、DataEase）。强调信息密度、状态色彩编码、实时动态效果。
- **反参考**: 避免过度装饰、避免玻璃拟态滥用、避免 generic 蓝色 SaaS 模板。

### Design Principles

1. **信息分层**: 核心指标 > 次要数据 > 辅助信息，重要数据大字号+发光效果
2. **密度优先**: 大屏环境下最大化信息密度，非大屏环境保持合理留白
3. **状态即用颜色编码**: 4级色彩系统（红/橙/蓝/绿），无需文字就能识别状态
4. **数据可视化**: 用图表代替表格，用趋势代替静态数值，用动画代替静止
5. **零横向滚动**: 所有面板自适应容器宽度，绝对不出现横向滚动条
6. **响应式布局**: 支持 1080p、1440p、2K 三档分辨率，关键面板在任何分辨率下均可用

### Technical Constraints
- **框架**: React 18 + TypeScript + Vite 5
- **UI 库**: Ant Design v5（ConfigProvider 统一 token，独立暗色 COLORS 常量用于大屏）
- **图表**: ECharts 5 + echarts-for-react
- **路由**: React Router v6
- **HTTP**: Axios
- **日期**: dayjs（中文 locale）
- **CSS**: 原生 CSS + CSS Variables（--pac-* 命名约定用于组件级 token）

### Accessibility
- 暗色主题需保证 WCAG AA 对比度（文本 `#ffffff` 在 `#0a1128` 上对比度 > 17:1 ✅）
- 状态颜色编码需配合文字标签，不依赖颜色单独传达信息
- 交互元素需有明确的 hover/focus/active 状态
